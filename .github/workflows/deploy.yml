# name: Deploy Vue.js App

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout Code
#       uses: actions/checkout@v3

#     - name: Deployment
#       uses: appleboy/ssh-action@v0.1.7
#       with:
#         host: ${{ secrets.SSH_HOST }}   
#         username: ${{ secrets.SSH_USERNAME }}    
#         # password: ${{ secrets.SSH_PASSWORD }}
#         key: ${{ secrets.SSH_KEY }}
#         script: |
#             cd /var/www/html
#             chmod +x /var/www/deploy.sh
#             /var/www/deploy.sh

#         debug: true                    # Enable debugging for more detailed logs
#         port: 22  


name: Deploy Vue.js App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '16'

    - name: Clean and Install Dependencies
      run: |
        rm -rf node_modules
        rm -f package-lock.json
        npm install --force --legacy-peer-deps

    - name: Build Vue.js App
      run: npm run build

    - name: List the Dist Directory
      run: ls -l dist/

    - name: Deploy Dist Folder to VPS
      uses: appleboy/ssh-action@v0.1.7
      with:
        host: ${{ secrets.SSH_HOST }}   
        username: ${{ secrets.SSH_USERNAME }}    
        key: ${{ secrets.SSH_KEY }}
        script: |
          # Navigate to the Vue.js app directory
          cd /var/www/html/bushman_frontend

          # Ensure that the user has proper permissions to modify files
          sudo chown -R $USER:$USER /var/www/html/bushman_frontend
          
          # Change permissions to ensure we can remove files
          sudo chmod -R 777 /var/www/html/bushman_frontend/*

          # Remove all files in the dist folder (including hidden files)
          sudo rm -rf /var/www/html/bushman_frontend/*

          # Copy the new build from GitHub Actions workspace to the VPS
          cp -r /home/runner/work/bushman_frontend/bushman_frontend/dist/* /var/www/html/bushman_frontend/

          # Ensure the proper file permissions for the dist folder
          sudo chmod -R 755 /var/www/html/bushman_frontend
        debug: true
        port: 22




